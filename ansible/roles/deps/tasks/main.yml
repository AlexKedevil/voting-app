---
- name: Mettre à jour le cache apt et installer python3-pip
  apt:
    name:
      - python3-pip
      - python3-setuptools
    state: present
    update_cache: yes

- name: Installer les packages pip requis
  pip:
    name: "{{ pip_packages }}"
    state: present
